#include <bits/stdc++.h>
using namespace std;
struct IR {
  int index, rank;
};
const int _N = 1e5 + 10;
IR i_rank[_N], tmp[_N];
int rank_i[_N], range = 1;
void _cmp_(IR l, IR r) {
}
inline void init() {}

main(void) {
  cin.tie(0);
  ios_base::sync_with_stdio(0);

  return 0;
}