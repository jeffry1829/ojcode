#include <bits/stdc++.h>
using namespace std;
const int _V = 1e3 + 10, _E = 2e4 + 10;
struct WE {
  int p, w;
};
int V, E, S, T, dis[_V], in_tree[_V];

inline void dijkstra() {
  priority_queue<int> pq;
  pq.push(S);
  for (int i = 0; i < V; i++) {
    int now;
    while ()
  }
}
main(void) {
  cin.tie(0);
  ios_base::sync_with_stdio(0);

  return 0;
}