#include <bits/stdc++.h>
using namespace std;

main(void) {
  cin.tie(0);
  ios_base::sync_with_stdio(0);
  int table[9][9];
  for (int i = 0; i < 9; i++)
    for (int j = 0; j < 9; j++)
      cin >> table[i][j];

  return 0;
}